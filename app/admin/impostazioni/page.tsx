'use client'

import { useEffect, useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, FileUp, KeyRound, Plus, Save } from 'lucide-react'

type CatalogItem = { id: string; plan_type: string; included_minutes: number; billing_cycle: string; price_cents: number }
export default function ImpostazioniAdmin() {
  const [catalog, setCatalog] = useState<CatalogItem[]>([]); const [providers, setProviders] = useState<any[]>([]); const [message, setMessage] = useState<string | null>(null); const [pdf, setPdf] = useState<File | null>(null)
  const [plan, setPlan] = useState({ planType: 'base', includedMinutes: '300', billingCycle: 'annuale', priceCents: '0' }); const [provider, setProvider] = useState({ provider: 'telnyx', label: '', secretReference: '' })
  async function load() { const [p, r] = await Promise.all([fetch('/api/admin/plans'), fetch('/api/admin/providers')]); const plans = await p.json(); setCatalog(plans.catalog || []); setProviders((await r.json()).providers || []) }
  useEffect(() => { load() }, [])
  async function savePlan(event: React.FormEvent) { event.preventDefault(); const response = await fetch('/api/admin/plans', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ ...plan, includedMinutes: Number(plan.includedMinutes), priceCents: Number(plan.priceCents) }) }); const body = await response.json(); setMessage(response.ok ? 'Prezzo salvato nel catalogo dinamico.' : body.error); if (response.ok) { setPlan({ ...plan, priceCents: '0' }); load() } }
  async function uploadPdf(event: React.FormEvent) { event.preventDefault(); if (!pdf) return; const data = new FormData(); data.set('file', pdf); const response = await fetch('/api/admin/plans/import', { method: 'POST', body: data }); const body = await response.json(); setMessage(response.ok ? `Importate ${body.imported} righe dal PDF.` : body.error); if (response.ok) load() }
  async function saveProvider(event: React.FormEvent) { event.preventDefault(); const response = await fetch('/api/admin/providers', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(provider) }); const body = await response.json(); setMessage(response.ok ? 'Riferimento provider salvato.' : body.error); if (response.ok) { setProvider({ provider: 'telnyx', label: '', secretReference: '' }); load() } }
  return <main className="min-h-screen bg-[#f7fbff] px-6 py-10 text-[var(--ink)] lg:px-10"><div className="mx-auto max-w-6xl"><Link href="/admin" className="inline-flex items-center gap-2 text-sm font-bold text-[var(--blue)]"><ArrowLeft size={16} /> Dashboard Admin</Link><div className="mt-8"><p className="text-sm font-bold uppercase tracking-[.18em] text-[var(--blue)]">Configurazione</p><h1 className="mt-2 text-4xl font-bold">Listini e provider</h1><p className="mt-3 max-w-2xl text-[var(--muted)]">Gestisci i prezzi per piano, minuti e ciclo di fatturazione. Le credenziali provider restano riferimenti ai secret di deploy.</p></div>{message && <p className="mt-6 rounded-xl bg-[#e5f8f6] px-4 py-3 text-sm font-semibold">{message}</p>}<div className="mt-8 grid gap-6 lg:grid-cols-2"><section className="rounded-3xl border border-[var(--line)] bg-white p-7"><h2 className="text-2xl font-bold">Gestione Listino Prezzi</h2><p className="mt-2 text-sm text-[var(--muted)]">Aggiungi o aggiorna una combinazione piano/minuti/ciclo.</p><form onSubmit={savePlan} className="mt-6 grid gap-4"><div className="grid grid-cols-2 gap-3"><select value={plan.planType} onChange={(e) => setPlan({ ...plan, planType: e.target.value })} className="rounded-2xl border border-[var(--line)] bg-white px-4 py-3"><option value="base">Base</option><option value="evoluto">Evoluto</option><option value="enterprise">Enterprise</option></select><select value={plan.includedMinutes} onChange={(e) => setPlan({ ...plan, includedMinutes: e.target.value })} className="rounded-2xl border border-[var(--line)] bg-white px-4 py-3">{[150,300,500,1000,2000,4000,7000,10000].map((m) => <option key={m} value={m}>{m.toLocaleString('it-IT')} min</option>)}</select></div><div className="grid grid-cols-2 gap-3"><select value={plan.billingCycle} onChange={(e) => setPlan({ ...plan, billingCycle: e.target.value })} className="rounded-2xl border border-[var(--line)] bg-white px-4 py-3"><option value="trimestrale">Trimestrale</option><option value="annuale">Annuale</option></select><input required type="number" min="0" step="0.01" placeholder="Prezzo € / mese" value={plan.priceCents === '0' ? '' : (Number(plan.priceCents) / 100).toString()} onChange={(e) => setPlan({ ...plan, priceCents: String(Math.round(Number(e.target.value || 0) * 100)) })} className="rounded-2xl border border-[var(--line)] px-4 py-3" /></div><button className="inline-flex items-center justify-center gap-2 rounded-full bg-[var(--ink)] px-5 py-3 font-bold text-white"><Plus size={16} /> Salva prezzo</button></form><form onSubmit={uploadPdf} className="mt-6 rounded-2xl border border-dashed border-[var(--blue)] bg-[#f7fbff] p-4"><p className="font-bold">Upload PDF Listino</p><p className="mt-1 text-xs text-[var(--muted)]">Parser automatico per PDF testuali con piano, minuti e prezzo.</p><input required type="file" accept="application/pdf" onChange={(e) => setPdf(e.target.files?.[0] || null)} className="mt-4 block w-full text-sm" /><button className="mt-4 inline-flex items-center gap-2 rounded-full border border-[var(--blue)] px-4 py-2 text-sm font-bold text-[var(--blue)]"><FileUp size={16} /> Importa PDF</button></form><div className="mt-8 max-h-72 space-y-2 overflow-auto">{catalog.map((item) => <div key={item.id} className="flex justify-between rounded-2xl bg-[#f7fbff] px-4 py-3 text-sm"><span className="font-bold capitalize">{item.plan_type} · {item.included_minutes} min · {item.billing_cycle}</span><span>€{(item.price_cents / 100).toFixed(2)}</span></div>)}</div></section><section className="rounded-3xl border border-[var(--line)] bg-white p-7"><div className="flex items-center gap-3"><KeyRound className="text-[var(--blue)]" /><div><h2 className="text-2xl font-bold">Provider API</h2><p className="mt-1 text-sm text-[var(--muted)]">Salva solo il nome del secret di deploy.</p></div></div><form onSubmit={saveProvider} className="mt-6 grid gap-4"><select value={provider.provider} onChange={(e) => setProvider({ ...provider, provider: e.target.value })} className="rounded-2xl border border-[var(--line)] bg-white px-4 py-3"><option value="telnyx">Telnyx</option><option value="vapi">Vapi</option><option value="elevenlabs">ElevenLabs</option><option value="deepgram">Deepgram</option></select><input required placeholder="Label (es. produzione)" value={provider.label} onChange={(e) => setProvider({ ...provider, label: e.target.value })} className="rounded-2xl border border-[var(--line)] px-4 py-3" /><input required placeholder="Variabile env (es. TELNYX_API_KEY)" value={provider.secretReference} onChange={(e) => setProvider({ ...provider, secretReference: e.target.value.toUpperCase() })} className="rounded-2xl border border-[var(--line)] px-4 py-3 font-mono text-sm" /><button className="inline-flex items-center justify-center gap-2 rounded-full bg-[var(--ink)] px-5 py-3 font-bold text-white"><Save size={16} /> Salva riferimento</button></form><div className="mt-8 space-y-2">{providers.map((item) => <div key={item.id} className="flex justify-between rounded-2xl bg-[#f7fbff] px-4 py-3 text-sm"><span className="font-bold">{item.provider} · {item.label}</span><code className="text-[var(--blue)]">{item.secret_reference}</code></div>)}</div></section></div></div></main>
}
