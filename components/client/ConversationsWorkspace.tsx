'use client'

import { useMemo, useState } from 'react'
import { Archive, Bot, Folder, Inbox, Mail, MessageCircle, MoreHorizontal, Phone, Plus, Search, Send, Settings2, ShieldCheck, Tag, Trash2, UserRound, Users, X, Zap } from 'lucide-react'

type Channel = 'WhatsApp' | 'Web' | 'Phone' | 'Email'
type Conversation = { id: string; name: string; company: string; preview: string; time: string; channel: Channel; status: 'Aperto' | 'In attesa'; label?: string; messages: { author: string; text: string; time: string; internal?: boolean }[] }

const seedConversations: Conversation[] = [
  { id: '1', name: 'Maria Rossi', company: 'Studio Medico Aurora', preview: 'Vorrei sapere se posso spostare l’appuntamento...', time: '09:42', channel: 'WhatsApp', status: 'Aperto', label: 'Da richiamare', messages: [{ author: 'Maria Rossi', text: 'Buongiorno, vorrei sapere se posso spostare l’appuntamento di domani.', time: '09:40' }, { author: 'ProntoAI24', text: 'Certo, verifico subito le disponibilità del medico.', time: '09:41' }] },
  { id: '2', name: 'Luca Bianchi', company: 'Bianchi & Figli', preview: 'Grazie, ho ricevuto il documento.', time: '08:58', channel: 'Web', status: 'Aperto', label: 'Supporto', messages: [{ author: 'Luca Bianchi', text: 'Grazie, ho ricevuto il documento.', time: '08:58' }] },
  { id: '3', name: 'Elena Conti', company: 'Privato', preview: 'Chiamata persa · 02:14', time: 'Ieri', channel: 'Phone', status: 'In attesa', messages: [{ author: 'Assistente vocale', text: 'Chiamata persa. Richiamare il contatto.', time: 'Ieri' }] },
  { id: '4', name: 'Paolo Verdi', company: 'Verdi Consulting', preview: 'Avrei bisogno di una fattura aggiornata.', time: 'Ieri', channel: 'Email', status: 'Aperto', label: 'Fatturazione', messages: [{ author: 'Paolo Verdi', text: 'Avrei bisogno di una fattura aggiornata.', time: 'Ieri' }] },
]

const channelIcon: Record<Channel, typeof MessageCircle> = { WhatsApp: MessageCircle, Web: MessageCircle, Phone, Email: Mail }
const labels = ['Da richiamare', 'Supporto', 'Fatturazione', 'Prompt']

export default function ConversationsWorkspace() {
  const [conversations, setConversations] = useState(seedConversations)
  const [selectedId, setSelectedId] = useState('1')
  const [query, setQuery] = useState('')
  const [status, setStatus] = useState<'Tutti' | 'Aperto' | 'In attesa'>('Aperto')
  const [channel, setChannel] = useState<'Tutti' | Channel>('Tutti')
  const [folder, setFolder] = useState('Tutte le cartelle')
  const [showFilters, setShowFilters] = useState(false)
  const [showFolders, setShowFolders] = useState(false)
  const [showLabels, setShowLabels] = useState(false)
  const [showSummary, setShowSummary] = useState(false)
  const [isHuman, setIsHuman] = useState(false)
  const [draft, setDraft] = useState('')
  const [internal, setInternal] = useState(false)

  const visible = useMemo(() => conversations.filter((item) => {
    const matchesQuery = `${item.name} ${item.company} ${item.preview}`.toLowerCase().includes(query.toLowerCase())
    const matchesStatus = status === 'Tutti' || item.status === status
    const matchesChannel = channel === 'Tutti' || item.channel === channel
    const matchesFolder = folder === 'Tutte le cartelle' || item.label === folder
    return matchesQuery && matchesStatus && matchesChannel && matchesFolder
  }), [channel, conversations, folder, query, status])
  const selected = conversations.find((item) => item.id === selectedId) ?? conversations[0]

  function sendMessage() {
    if (!draft.trim() || !selected) return
    setConversations((current) => current.map((item) => item.id === selected.id ? { ...item, preview: draft, time: 'Adesso', messages: [...item.messages, { author: internal ? 'Nota interna' : 'ProntoAI24', text: draft, time: 'Adesso', internal }] } : item))
    setDraft('')
  }

  function summarize() { setShowSummary(true) }
  function removeSelected() { if (selected) setConversations((current) => current.filter((item) => item.id !== selected.id)) }

  return <main className="min-h-[calc(100vh-80px)] bg-[#f7fbff] text-[var(--ink)]">
    <header className="flex flex-wrap items-center justify-between gap-4 border-b border-[var(--line)] bg-white px-5 py-5 lg:px-8"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--blue)]">Spazio operativo</p><h1 className="mt-1 text-3xl font-bold">Conversazioni</h1></div><div className="flex items-center gap-2"><button onClick={() => setShowFilters(true)} className="inline-flex items-center gap-2 rounded-full border border-[var(--line)] bg-white px-4 py-2 text-sm font-bold hover:border-[var(--cyan)]"><Settings2 size={16} /> Filtri</button><button className="inline-flex items-center gap-2 rounded-full bg-[var(--ink)] px-4 py-2 text-sm font-bold text-white"><Zap size={16} className="text-[var(--lime)]" /> Automazioni</button></div></header>
    <div className="grid min-h-[700px] lg:grid-cols-[220px_360px_minmax(0,1fr)]">
      <aside className="border-b border-[var(--line)] bg-white p-4 lg:border-b-0 lg:border-r"><nav className="grid gap-1"><button className="flex items-center gap-3 rounded-xl bg-[#eaf8fa] px-3 py-3 text-left text-sm font-bold text-[var(--blue)]"><Inbox size={17} /> Tutti <span className="ml-auto text-xs">24</span></button><button className="flex items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-semibold text-[var(--muted)] hover:bg-[var(--paper)]"><UserRound size={17} /> Assegnato a me</button></nav><div className="mt-8"><div className="flex items-center justify-between px-3"><p className="text-xs font-bold uppercase tracking-[.14em] text-[var(--muted)]">Colleghi</p><Users size={15} className="text-[var(--muted)]" /></div><div className="mt-3 grid gap-2 px-3 text-sm text-[var(--muted)]"><span className="flex items-center gap-2"><i className="h-2 w-2 rounded-full bg-[var(--cyan)]" /> ProntoAI Voice</span><span className="flex items-center gap-2"><i className="h-2 w-2 rounded-full bg-[var(--lime)]" /> Team supporto</span></div></div><div className="mt-8"><div className="flex items-center justify-between px-3"><p className="text-xs font-bold uppercase tracking-[.14em] text-[var(--muted)]">Cartelle</p><button onClick={() => setShowFolders(true)} aria-label="Aggiungi cartella"><Plus size={16} className="text-[var(--blue)]" /></button></div><div className="mt-3 grid gap-1"><button onClick={() => setFolder('Tutte le cartelle')} className={`flex items-center gap-3 rounded-xl px-3 py-2 text-left text-sm font-semibold ${folder === 'Tutte le cartelle' ? 'bg-[var(--paper)]' : 'text-[var(--muted)]'}`}><Folder size={16} /> Tutte le cartelle</button>{labels.slice(0, 3).map((item) => <button key={item} onClick={() => setFolder(item)} className={`flex items-center gap-3 rounded-xl px-3 py-2 text-left text-sm font-semibold ${folder === item ? 'bg-[var(--paper)]' : 'text-[var(--muted)]'}`}><Folder size={16} /> {item}</button>)}</div></div></aside>
      <section className="border-b border-[var(--line)] bg-[#fbfdff] lg:border-b-0 lg:border-r"><div className="border-b border-[var(--line)] p-4"><div className="flex items-center gap-2 rounded-xl border border-[var(--line)] bg-white px-3 py-2"><Search size={17} className="text-[var(--muted)]" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Cerca conversazioni" className="min-w-0 flex-1 bg-transparent text-sm outline-none" /></div><div className="mt-4 flex items-center justify-between"><select value={status} onChange={(e) => setStatus(e.target.value as typeof status)} className="rounded-lg bg-transparent text-sm font-bold outline-none"><option>Tutti</option><option>Aperto</option><option>In attesa</option></select><select value={channel} onChange={(e) => setChannel(e.target.value as typeof channel)} className="rounded-lg bg-transparent text-sm font-semibold text-[var(--muted)] outline-none"><option>Tutti</option><option>WhatsApp</option><option>Web</option><option>Phone</option><option>Email</option></select><button onClick={removeSelected} title="Elimina conversazione selezionata" className="rounded-lg p-2 text-[var(--muted)] hover:bg-red-50 hover:text-red-600"><Trash2 size={16} /></button></div></div><div className="divide-y divide-[var(--line)]">{visible.map((item) => { const Icon = channelIcon[item.channel]; return <button key={item.id} onClick={() => setSelectedId(item.id)} className={`w-full p-4 text-left transition hover:bg-white ${item.id === selected?.id ? 'border-l-4 border-[var(--cyan)] bg-white' : ''}`}><div className="flex items-start gap-3"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-[#eaf0ff] text-[var(--blue)]"><Icon size={17} /></span><span className="min-w-0 flex-1"><span className="flex items-center justify-between gap-2"><strong className="truncate text-sm">{item.name}</strong><time className="shrink-0 text-[10px] font-bold text-[var(--muted)]">{item.time}</time></span><span className="mt-1 block truncate text-xs text-[var(--muted)]">{item.company}</span><span className="mt-2 block truncate text-sm text-[var(--muted)]">{item.preview}</span>{item.label && <span className="mt-2 inline-flex rounded-full bg-[#eff9d8] px-2 py-1 text-[10px] font-bold text-[#567318]">{item.label}</span>}</span></div></button> })}{visible.length === 0 && <p className="p-8 text-center text-sm text-[var(--muted)]">Nessuna conversazione corrisponde ai filtri.</p>}</div></section>
      {selected ? <section className="flex min-h-[700px] min-w-0 flex-col bg-white"><header className="flex flex-wrap items-center justify-between gap-3 border-b border-[var(--line)] px-5 py-4"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-2xl bg-[#eaf8fa] font-bold text-[var(--blue)]">{selected.name.slice(0, 1)}</span><div><h2 className="font-bold">{selected.name}</h2><p className="text-xs text-[var(--muted)]">{selected.company} · {selected.channel}</p></div></div><div className="flex items-center gap-1"><button onClick={() => setShowFolders(true)} className="rounded-xl p-2 text-[var(--muted)] hover:bg-[var(--paper)]" title="Cartella"><Folder size={18} /></button><button onClick={() => setShowLabels(true)} className="rounded-xl p-2 text-[var(--muted)] hover:bg-[var(--paper)]" title="Etichetta"><Tag size={18} /></button><button onClick={summarize} className="rounded-xl p-2 text-[var(--muted)] hover:bg-[var(--paper)]" title="Riassumi"><Bot size={18} /></button><button onClick={removeSelected} className="rounded-xl p-2 text-[var(--muted)] hover:bg-red-50 hover:text-red-600" title="Elimina"><Trash2 size={18} /></button><button className="rounded-xl p-2 text-[var(--muted)] hover:bg-[var(--paper)]" title="Profilo contatto"><MoreHorizontal size={18} /></button></div></header><div className="flex-1 space-y-5 overflow-y-auto p-5 lg:p-8">{selected.messages.map((message, index) => <div key={`${message.time}-${index}`} className={`flex ${message.author === 'ProntoAI24' ? 'justify-end' : 'justify-start'}`}><div className={`max-w-[78%] rounded-2xl px-4 py-3 text-sm leading-6 ${message.internal ? 'border border-[#ead8bd] bg-[#fff8ec]' : message.author === 'ProntoAI24' ? 'bg-[var(--ink)] text-white' : 'bg-[var(--paper)]'}`}><div className="mb-1 flex items-center gap-2 text-[10px] font-bold opacity-60"><span>{message.author}</span>{message.internal && <span>Nota interna</span>}<span>{message.time}</span></div>{message.text}</div></div>)}</div><div className="border-t border-[var(--line)] p-4"><div className="mb-3 flex items-center justify-between rounded-xl bg-[#f7fbff] px-3 py-2 text-xs font-bold"><span className="flex items-center gap-2 text-[var(--muted)]"><ShieldCheck size={15} className="text-[var(--cyan)]" /> {isHuman ? 'Stai gestendo tu questa conversazione' : 'ProntoAI sta gestendo questa conversazione'}</span><button onClick={() => setIsHuman(!isHuman)} className="text-[var(--blue)]">{isHuman ? 'Restituisci all’AI' : 'Prendi il controllo'}</button></div><div className="flex items-end gap-2"><button onClick={() => setInternal(!internal)} className={`rounded-xl p-3 ${internal ? 'bg-[#fff0dc] text-[#925c0c]' : 'bg-[var(--paper)] text-[var(--muted)]'}`} title="Nota interna"><Archive size={18} /></button><textarea value={draft} onChange={(e) => setDraft(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); sendMessage() } }} placeholder={internal ? 'Scrivi una nota interna…' : 'Scrivi una risposta…'} className="min-h-11 flex-1 resize-none rounded-xl border border-[var(--line)] px-3 py-3 text-sm outline-none focus:border-[var(--cyan)]" /><button onClick={sendMessage} className="rounded-xl bg-[var(--ink)] p-3 text-white" aria-label="Invia messaggio"><Send size={18} /></button></div></div></section> : <section className="flex items-center justify-center p-8 text-[var(--muted)]">Seleziona una conversazione</section>}
    </div>
    {showFilters && <Modal title="Filtri avanzati" onClose={() => setShowFilters(false)}><div className="grid gap-4"><label className="text-sm font-bold">Canale<select value={channel} onChange={(e) => setChannel(e.target.value as typeof channel)} className="mt-2 w-full rounded-xl border border-[var(--line)] p-3 font-normal"><option>Tutti</option><option>WhatsApp</option><option>Web</option><option>Phone</option><option>Email</option></select></label><label className="text-sm font-bold">Etichetta<select value={folder} onChange={(e) => setFolder(e.target.value)} className="mt-2 w-full rounded-xl border border-[var(--line)] p-3 font-normal"><option>Tutte le cartelle</option>{labels.map((label) => <option key={label}>{label}</option>)}</select></label><button onClick={() => setShowFilters(false)} className="rounded-full bg-[var(--ink)] px-4 py-3 font-bold text-white">Applica filtri</button></div></Modal>}
    {showFolders && <Modal title="Cartelle" onClose={() => setShowFolders(false)}><p className="text-sm leading-6 text-[var(--muted)]">Organizza le conversazioni in cartelle condivise dal team.</p><button onClick={() => { setFolder('Supporto'); setShowFolders(false) }} className="mt-5 flex w-full items-center gap-3 rounded-xl border border-[var(--line)] p-3 text-left text-sm font-bold"><Plus size={16} /> Crea o assegna cartella</button></Modal>}
    {showLabels && <Modal title="Etichette" onClose={() => setShowLabels(false)}><div className="flex flex-wrap gap-2">{labels.map((label) => <button key={label} onClick={() => { setFolder(label); setShowLabels(false) }} className="rounded-full bg-[#eff9d8] px-3 py-2 text-xs font-bold text-[#567318]">{label}</button>)}</div></Modal>}
    {showSummary && <Modal title="Riassunto AI" onClose={() => setShowSummary(false)}><p className="rounded-2xl bg-[var(--paper)] p-4 text-sm leading-7">{selected?.name} ha richiesto supporto operativo. L’assistente ha preso in carico la richiesta e il prossimo passo consigliato è verificare la disponibilità e confermare il contatto.</p><button onClick={() => navigator.clipboard?.writeText('Riassunto conversazione: richiesta di supporto operativo.')} className="mt-4 rounded-full border border-[var(--line)] px-4 py-2 text-sm font-bold">Copia riassunto</button></Modal>}
  </main>
}

function Modal({ title, onClose, children }: { title: string; onClose: () => void; children: React.ReactNode }) { return <div className="fixed inset-0 z-50 flex items-center justify-center bg-[var(--ink)]/50 p-4" role="dialog" aria-modal="true"><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><h2 className="text-2xl font-bold">{title}</h2><button onClick={onClose} aria-label="Chiudi" className="rounded-xl p-2 text-[var(--muted)] hover:bg-[var(--paper)]"><X size={18} /></button></div><div className="mt-5">{children}</div></div></div> }
